# L-14 README·dogfood

## 목적
문서와 자기 저장소 적용.

## 파일
- `README.md` 🔁 Autonomous Loop Engine 섹션: 흐름도의 시작 단계를 `omh loop start`로, "Operating it" 블록을 `omh loop start|status|stop [--now]|clean [--branch]`로, 설정 예시에 `limitBackoff/emptyBackoff/stallStreak/turnTimeout` 추가, 생성물 트리에서 `.omh/loop/run.sh` 제거·`.omh/state/loop/` 추가, "POSIX only" 1줄, D7 한계 1줄("실행 중 메인 원장을 편집하면 다음 재시작에 재시드").
- dogfood: `npm run build && node dist/bin/oh-my-harness.js sync` → `.claude/skills/omh-loop/SKILL.md` 존재, `.omh/loop/run.sh` 없음, `node dist/bin/oh-my-harness.js sync --check` = up to date.

## 검증
`grep -c "run.sh" README.md` → 0; `test ! -e .omh/loop/run.sh`; `node dist/bin/oh-my-harness.js sync --check`

## 공통 규칙
- TDD: 테스트 파일 먼저 작성 → `npx vitest run <테스트파일>`로 실패 확인 → 최소 구현 → 통과.
- 커밋 전 `npm run lint && npx vitest run` 통과. 커밋 1개, 메시지 첫 줄 `loop: L-14 README·dogfood`.
- 지시서에 없는 파일은 만들거나 고치지 않는다. 설계 판단이 필요하면 `BLOCKED: <질문>` 표기.
- 원장 `WORKPLAN-LOOP.md` §5의 체크박스와 §7 진행 로그 1줄을 같은 커밋에서 갱신.
