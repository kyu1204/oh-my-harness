# L-17 실 QA — Codex (아키텍트 실행)

## 목적
실제 `codex` 런타임으로 G7 ①~⑦ 검증. **루프 실행 금지 영역** — 아키텍트가 tmux에서 직접 수행.

## 절차
1. `DIR=$(mktemp -d) && bash scripts/loop-qa-fixture.sh codex "$DIR"`
2. `tmux new -d -s omh-qa-codex "cd $DIR && node <repo>/dist/bin/oh-my-harness.js loop start"`
3. **즉시 모니터 2개 부착**: `tail -F $DIR/.omh/state/loop/runs/*/events.jsonl` / `tail -n 0 -F $DIR/.git/logs/HEAD` (+ worktree `.git/worktrees/*/logs/HEAD`). empty commit으로 발화 테스트.
4. 관찰·기록(§7 진행 로그에 발췌):
   - ① start 즉시 반환, run.json 존재
   - ② 이벤트 `progress` + omh-loop 브랜치 커밋 해시
   - ③ worktree 원장 Q-1 체크
   - ④ Q-3에서 `.omh/state/events.jsonl`(worktree 쪽)에 `catalog-loop-guard` block + 원장에 `BLOCKED: architect-only`
   - ⑤ Q-4 후 `complete` 이벤트, `kill -0 -<pid>` ESRCH
   - ⑥ 별도 run: Q-4 지시서를 `sleep 120` 포함으로 바꿔 start → `loop stop --now` → 15s 내 그룹 소멸
   - ⑦ 이벤트 로그에 blocked 이벤트가 Q-3 1건뿐(프롬프트 에코 오판 0건)
   
5. 실패 시: §6 리스크에 기록, 원인 태스크 `L-XX` 신설(아키텍트 지시서) → 수정 → 재실행.

## 검증
G7 항목 전부 체크된 발췌가 §7에 기록됨.

## 공통 규칙
- TDD: 테스트 파일 먼저 작성 → `npx vitest run <테스트파일>`로 실패 확인 → 최소 구현 → 통과.
- 커밋 전 `npm run lint && npx vitest run` 통과. 커밋 1개, 메시지 첫 줄 `loop: L-17 실 QA — Codex (아키텍트 실행)`.
- 지시서에 없는 파일은 만들거나 고치지 않는다. 설계 판단이 필요하면 `BLOCKED: <질문>` 표기.
- 원장 `WORKPLAN-LOOP.md` §5의 체크박스와 §7 진행 로그 1줄을 같은 커밋에서 갱신.
