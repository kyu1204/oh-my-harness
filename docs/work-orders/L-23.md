# L-23 명시적 loop-guard의 loop 경로 강제

## 지시
`harness-converter-v2.ts`: loop 활성 시 hooks에 명시적 `loop-guard`가 있으면 그 항목의 `params.workOrders`/`params.architectOnly`를 loop 설정값으로 **덮어쓴다**(mode는 유지). 테스트(`loop-guard.test.ts` wiring): `loop.workOrders: "orders"` + `hooks: [{block: loop-guard, params: {}}]` → 병합된 preToolUse의 loop-guard 스크립트에 `orders`가 들어감; `architectOnly`도 동일.

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 `npm run lint && npx vitest run`. 메시지 `loop: L-23 명시적 loop-guard의 loop 경로 강제`. 원장 체크박스·§7 같은 커밋.
