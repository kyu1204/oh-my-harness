# L-24 uninstall 계획 단계 부작용 제거

## 지시
`computeUninstall`에서 `stopLoop`/`removeWorktree` 호출을 제거하고 `applyUninstallPlan` 시작부(삭제 전)로 옮긴다. `--dry-run`·확인 취소는 러너를 건드리지 않는다. 테스트(`uninstall-loop.test.ts`): computeUninstall 후 worktree 존재 유지; applyUninstallPlan 후 제거. 이벤트 로그에 stop 플래그가 계획 단계에서 생기지 않음.

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 `npm run lint && npx vitest run`. 메시지 `loop: L-24 uninstall 계획 단계 부작용 제거`. 원장 체크박스·§7 같은 커밋.
