# L-26 classify/ledger 판정 수정

## 지시
(a) limit 판정을 exit 상태 분기보다 먼저(단 timedOut→crash 최우선, !progress 조건 유지) — 리밋 메시지는 비정상 종료와 함께 오는 게 보통. (b) 'error'도 stall streak에 포함 — 연속 에러가 interval 전속으로 무한 재시도되는 것 방지. (c) parseLedger가 '+' 불릿도 인식. 테스트: limit+exit1→limit, error×3→blockedBackoff, '+ [ ] x' 카운트.

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 전체 테스트·tsc. 메시지 `loop: L-26 ...`. 원장 갱신 동커밋.
