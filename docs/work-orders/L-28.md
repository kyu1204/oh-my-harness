# L-28 supervisor/runtime 그룹·git 실패 처리

## 지시
(a) 슈퍼바이저 finally에서 마지막 턴 그룹에 SIGKILL 스윕(TERM 무시 데몬 잔존 방지; sweepOwnGroup은 유지하되 턴은 자기 세션이라 도달 불가함을 주석에 명시). (b) headOf/스냅샷 git 실패를 try로 감싸 'failed' 이벤트 + 정상 종료. (c) start 프리플라이트의 currentBranch를 try로 감싸 unborn-HEAD 등은 fail() 메시지로.

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 전체 테스트·tsc. 메시지 `loop: L-28 ...`. 원장 갱신 동커밋.
