# L-29 worktree/generator 데이터 보호

## 지시
(a) seedLedger 재시드가 기존 worktree 원장을 덮기 전 `<ledger>.pre-seed-<runId>`로 백업. (b) generate()의 loop 비활성화 경로: worktree가 dirty면 제거하지 않고 경고 출력(명시적 파괴는 omh loop clean/uninstall만).

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 전체 테스트·tsc. 메시지 `loop: L-29 ...`. 원장 갱신 동커밋.
