# L-30 guard/schema 회귀 봉합

## 지시
(a) relPath에 셸 메타문자 금지 재도입(따옴표·백틱·$·백슬래시) — 이유 주석: 값이 loop-guard bash에 렌더됨(argv 전환으로 풀었던 것의 회귀, critical). (b) sentinel: trim 불변·개행 금지 refine. (c) loop-guard apply_patch 파싱에 `*** Move to: <path>` 목적지 포함.

## 공통 규칙
- TDD: 테스트 먼저 → 실패 확인 → 최소 구현. 커밋 전 전체 테스트·tsc. 메시지 `loop: L-30 ...`. 원장 갱신 동커밋.
